# Claude Code Trading System - Environment Variables Example
# Copy this file to .env and update with your actual credentials

# Database Type: Set to 'postgresql' or 'sqlite'
DB_TYPE=postgresql

# PostgreSQL Configuration (if using PostgreSQL)
# Default credentials (update these with your actual values if different)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=freqtrade_db
POSTGRES_USER=freqtrade_user
POSTGRES_PASSWORD=Vontarius97$

# SQLite Configuration (if using SQLite)
SQLITE_PATH=freqtrade.db

# Usage:
# 1. Copy this file: cp env.example.txt .env
# 2. Edit .env with your actual credentials
# 3. Set environment variables before running: source .env (or export variables)
# 4. Run the system
# 
# Alternative: You can also edit config/system_config.py directly
# with your credentials, but using environment variables is recommended
# for security and deployment flexibility.
